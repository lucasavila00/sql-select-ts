name: Static Analysis
on: [push]
jobs:
    static_analysis:
        runs-on: ubuntu-20.04
        steps:
            - name: Checkout 🛎️
              uses: actions/checkout@v3
            - name: Static checks
              run: |
                  make install
                  make static
