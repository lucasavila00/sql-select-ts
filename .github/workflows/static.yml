name: Static analysis
on: [push]
jobs:
    test-lib:
        runs-on: ubuntu-20.04
        concurrency: ci-${{ github.ref }}
        steps:
            - name: Checkout 🛎️
              uses: actions/checkout@v3
            - name: Static checks
              run: |
                  make install
                  make static
