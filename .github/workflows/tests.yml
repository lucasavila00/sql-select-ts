name: Test lib
on: [push]
jobs:
    test-lib:
        runs-on: ubuntu-20.04
        steps:
            - name: Checkout 🛎️
              uses: actions/checkout@v3
            - name: Tests
              run: |
                  make install
                  make test
